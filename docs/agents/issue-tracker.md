# Local Markdown Issue Tracker

Issues are stored as local markdown files under `.scratch/<feature>/`.

## Usage
- Each issue is a `.md` file inside `.scratch/<feature>/`.
- Skills like `to-tickets`, `triage`, and `to-spec` read and write issue files here.
